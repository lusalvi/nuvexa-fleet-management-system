apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nuvexa.fullname" . }}-test-connection"
  labels:
    {{- include "nuvexa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nuvexa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
