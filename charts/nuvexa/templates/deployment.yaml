{{- $name := .Values.component }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-{{ $name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $name }}
  template:
    metadata:
      labels:
        app: {{ $name }}
    spec:
      containers:
        - name: {{ $name }}
          image: {{ .Values.image }}
          ports:
            - containerPort: {{ .Values.port }}
          {{- if eq $name "vehiculos" }}
          env:
            - name: SUPABASE_URL
              value: {{ .Values.supabase.url }}
            - name: SUPABASE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.supabase.secretName }}
                  key: {{ .Values.supabase.secretKey }}
          {{- end }}